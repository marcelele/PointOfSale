The javadoc documentation is in doc/ folder, you can run it by simply opening the "index.html" file.
To run the program tests, you can either build it via gradle, 
or open the project with IntelliJ Idea and use the existing build configuration.